{
	"name": "tree-sitter-lua",
	"version": "0.1.0",
	"description": "Lua grammar for tree-sitter",
	"main": "index.js",
	"devDependencies": {
		"tree-sitter-cli": "^0.12.6"
	},
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test && tree-sitter parse examples/* -q -t --repeat 10"
  }
}
